---
title: Code
description: Code captures a short, fixed-length verification code.
---

import { Ic } from '@/components/nm/icon';
import { CodeSandbox } from '@/components/nm/code-sandbox';

<p className="eyebrow">Components · Form</p>

<p className="lede">A code field captures a short, fixed-length value — a one-time password, a PIN, a signing confirmation code. Namirial doesn't give it dedicated tokens; it's a composition of single-character <a href="/docs/components/form/input">input</a> cells laid out in a row, so it inherits sizes, focus, and validation for free.</p>

<div className="hero-stage">
  <div className="code-field">
    <div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="4" /></div></div>
    <div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="2" /></div></div>
    <div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="9" /></div></div>
    <div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} placeholder="-" /></div></div>
    <div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} placeholder="-" /></div></div>
    <div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} placeholder="-" /></div></div>
  </div>
</div>

<section className="block" id="overview">

## Overview

The Code Field component is designed for entering short verification codes, where each character is entered into its own input cell. This layout improves readability, makes input errors easier to spot, and supports common authentication and verification flows.

### When to use
Use the Code Field component when:
- Users need to enter a one-time password (OTP) received via SMS, email, or an authenticator app.
- Users need to enter or confirm a short PIN or verification code.
- A workflow requires a short confirmation or signing code as part of a secure process.

</section>

<section className="block" id="anatomy">

## Anatomy

Each cell is a full `.nm-input` instance — container and field — with `maxlength="1"` and centered text, laid out with a simple flex row. There is no `.nm-code` class in the theme; the row is just `display: flex` with a gap.

<div className="anatomy" aria-hidden="true">
  <div className="stage" style={{width:'640px',height:'220px'}}>
    <div className="code-field giant" style={{pointerEvents:'none',position:'absolute',top:'60px',left:'220px'}}>
      <div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="4" /></div></div>
      <div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="2" /></div></div>
      <div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} placeholder="-" /></div></div>
    </div>
    <svg style={{position:'absolute',top:0,left:0,width:'640px',height:'220px',pointerEvents:'none'}} viewBox="0 0 640 220">
      <line x1="100" y1="40" x2="240" y2="45" stroke="var(--palette-accent-500)" strokeWidth="1.5" strokeDasharray="3 3" opacity="0.6" />
      <line x1="480" y1="175" x2="244" y2="84" stroke="var(--palette-accent-500)" strokeWidth="1.5" strokeDasharray="3 3" opacity="0.6" />
    </svg>
    <div style={{position:'absolute',top:'20px',left:'60px'}}>
      <div className="labelpoint" style={{position:'static'}}>1 · <b>Cell</b></div>
      <div className="dot" style={{top:'25px',left:'180px'}}></div>
    </div>
    <div style={{position:'absolute',top:'170px',left:'420px'}}>
      <div className="labelpoint" style={{position:'static'}}>2 · <b>Row gap: 8px</b></div>
      <div className="dot" style={{top:'-86px',left:'-176px'}}></div>
    </div>
  </div>
</div>

#### Tokens and values that drive each part

| Part | Role | Source |
|------|------|--------|
| Cell | Every visual property | `--nm-input-*` — identical to a size-matched input (see [Input](/docs/components/form/input)) |
| Cell width | Fixed so digits don't reflow | `32px`, same at every size |
| Cell | Border radius, both sizes | `8px` |
| Row | Gap between cells | `8px` |
| Text | Centered in the cell | `text-align: center` on `.nm-input__field` |

</section>

<section className="block" id="sizes">

## Sizes

Code only supports two sizes — `sm` and `lg` — pick `lg` for signing flows where legibility at a glance matters most.

<div className="scale-row"><span className="sz">sm</span><div><div className="code-field"><div className="nm-input nm-input--sm"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="4" /></div></div><div className="nm-input nm-input--sm"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="2" /></div></div><div className="nm-input nm-input--sm"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="9" /></div></div><div className="nm-input nm-input--sm"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} placeholder="-" /></div></div></div></div><div className="metric">in-page confirmations</div></div>
<div className="scale-row"><span className="sz">lg</span><div><div className="code-field"><div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="4" /></div></div><div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="2" /></div></div><div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="9" /></div></div><div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} placeholder="-" /></div></div></div></div><div className="metric">default for OTP, signing surfaces</div></div>

</section>

<section className="block" id="states">

## States

Each cell carries its own focus ring, so the currently-active cell is unambiguous. Validation is applied to the whole row at once, not per cell.

#### Focus state
<p className="caption">First digit entered, second cell focused and awaiting input</p>

<div className="example">
  <div className="code-field">
    <div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="4" readOnly /></div></div>
    <div className="nm-input nm-input--lg"><div className="nm-input__container" data-state="focus"><input className="nm-input__field" maxLength={1} placeholder="-" readOnly /></div></div>
    <div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} placeholder="-" readOnly /></div></div>
    <div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} placeholder="-" readOnly /></div></div>
  </div>
</div>

<p className="caption">Error — all four cells recolor together with <code>nm-input--error</code>, with a status icon alongside the row</p>

#### Error states

All code cells recolor together with <code>nm-input--error</code>. 

<div className="example">
  <div className="nm-input--error" style={{display:'flex',flexDirection:'column',alignItems:'flex-start',gap:'8px'}}>
    <div className="code-field">
      <div className="nm-input nm-input--lg nm-input--error"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="4" readOnly /></div></div>
      <div className="nm-input nm-input--lg nm-input--error"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="2" readOnly /></div></div>
      <div className="nm-input nm-input--lg nm-input--error"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="9" readOnly /></div></div>
      <div className="nm-input nm-input--lg nm-input--error"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="1" readOnly /></div></div>
    </div>
    <div className="nm-input__help" style={{color:'var(--nm-input-help-fg)', display:'flex', alignItems:'center', gap:'4px'}}><Ic n="alert-circle" /> Incorrect code. Please try again.</div>
  </div>
</div>

<p className="caption">Error message - placed directly beneath the row, exactly like a standard input fields.</p>

<div className="example">
  <div style={{display:'flex',flexDirection:'column',alignItems:'flex-start',gap:'8px'}}>
    <div className="code-field">
      <div className="nm-input nm-input--lg nm-input--error"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="4" readOnly /></div></div>
      <div className="nm-input nm-input--lg nm-input--error"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="2" readOnly /></div></div>
      <div className="nm-input nm-input--lg nm-input--error"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="9" readOnly /></div></div>
      <div className="nm-input nm-input--lg nm-input--error"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="1" readOnly /></div></div>
    </div>
    <div className="nm-input__help">Code expires in <b>4:55</b></div>
    <div className="nm-input__help nm-input--error" style={{color:'var(--nm-input-help-fg)', display:'flex', alignItems:'center', gap:'4px'}}><Ic n="alert-circle" /> Incorrect code. Please try again.</div>
  </div>
</div>

<p className="caption">Help text + error — If persistent help text (e.g. a countdown) is shown, keep it in its neutral color and display the error on a second line below it. This keeps the countdown visible during validation.</p>

#### Success state
<p className="caption">Success — shown once the whole code validates.</p>

<div className="example">
  <div className="nm-input--success" style={{display:'flex',flexDirection:'column',alignItems:'flex-start',gap:'8px'}}>
    <div className="code-field">
      <div className="nm-input nm-input--lg nm-input--success"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="4" readOnly /></div></div>
      <div className="nm-input nm-input--lg nm-input--success"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="2" readOnly /></div></div>
      <div className="nm-input nm-input--lg nm-input--success"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="9" readOnly /></div></div>
      <div className="nm-input nm-input--lg nm-input--success"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="1" readOnly /></div></div>
    </div>
    <div className="nm-input__help" style={{color:'var(--nm-input-help-fg)', display:'flex', alignItems:'center', gap:'4px'}}><Ic n="circle-check" /> Code verified.</div>
  </div>
</div>

</section>

<section className="block" id="behavior">

## Behavior

A code field needs a small amount of JavaScript beyond CSS: typing a digit should auto-advance focus to the next cell, Backspace on an empty cell should move focus back, and pasting a full code should split it across every cell.

```js
const cells = Array.from(row.querySelectorAll('.nm-input__field'));

cells.forEach((cell, i) => {
  cell.addEventListener('input', () => {
    cell.value = cell.value.replace(/\D/g, '').slice(0, 1);
    if (cell.value && cells[i + 1]) cells[i + 1].focus();
  });
  cell.addEventListener('keydown', (e) => {
    if (e.key === 'Backspace' && !cell.value && cells[i - 1]) cells[i - 1].focus();
  });
  cell.addEventListener('paste', (e) => {
    const digits = e.clipboardData.getData('text').replace(/\D/g, '').split('');
    if (digits.length < 2) return;
    e.preventDefault();
    digits.slice(0, cells.length).forEach((d, j) => { cells[j].value = d; });
    cells[Math.min(digits.length, cells.length) - 1].focus();
  });
});
```

Validate and submit only once every cell is filled — don't ask the user to press a separate "Verify" button for a 4–6 digit code if you can submit automatically on completion.

</section>

<section className="block" id="dark">

## Light & dark theme

<div className="theme-pair">
  <div className="theme-card" data-theme="light">
    <div className="theme-label">Light <span>data-theme="light"</span></div>
    <div className="theme-frame">
      <div className="code-field">
        <div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="4" readOnly /></div></div>
        <div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="2" readOnly /></div></div>
        <div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} placeholder="-" readOnly /></div></div>
        <div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} placeholder="-" readOnly /></div></div>
      </div>
    </div>
  </div>
  <div className="theme-card" data-theme="dark">
    <div className="theme-label">Dark <span>data-theme="dark"</span></div>
    <div className="theme-frame">
      <div className="code-field">
        <div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="4" readOnly /></div></div>
        <div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="2" readOnly /></div></div>
        <div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} placeholder="-" readOnly /></div></div>
        <div className="nm-input nm-input--lg"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} placeholder="-" readOnly /></div></div>
      </div>
    </div>
  </div>
</div>

</section>

<section className="block" id="dodont">

## Do & don't

<div className="dodont">
  <div className="card do"><div className="bar"><span className="pill">✓</span>Do</div><div className="frame"><div className="code-field"><div className="nm-input nm-input--sm"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="4" readOnly /></div></div><div className="nm-input nm-input--sm"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="2" readOnly /></div></div><div className="nm-input nm-input--sm"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="9" readOnly /></div></div><div className="nm-input nm-input--sm"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="1" readOnly /></div></div></div></div><div className="desc">Support pasting the entire code into any cell — split it automatically across the row.</div></div>
  <div className="card dont"><div className="bar"><span className="pill">✕</span>Don't</div><div className="frame"><div className="code-field"><div className="nm-input nm-input--sm"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} placeholder="-" readOnly disabled /></div></div><div className="nm-input nm-input--sm"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} placeholder="-" readOnly disabled /></div></div><div className="nm-input nm-input--sm"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} placeholder="-" readOnly disabled /></div></div><div className="nm-input nm-input--sm"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} placeholder="-" readOnly disabled /></div></div></div></div><div className="desc">Don't block paste or autofill — users copy codes from SMS, email, and password managers.</div></div>
</div>

<div className="dodont">
  <div className="card do"><div className="bar"><span className="pill">✓</span>Do</div><div className="frame"><div className="code-field"><div className="nm-input nm-input--sm"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="4" readOnly /></div></div><div className="nm-input nm-input--sm"><div className="nm-input__container" data-state="focus"><input className="nm-input__field" maxLength={1} placeholder="-" readOnly /></div></div><div className="nm-input nm-input--sm"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} placeholder="-" readOnly /></div></div><div className="nm-input nm-input--sm"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} placeholder="-" readOnly /></div></div></div></div><div className="desc">Auto-advance focus to the next cell after each valid character is typed.</div></div>
  <div className="card dont"><div className="bar"><span className="pill">✕</span>Don't</div><div className="frame"><div className="code-field"><div className="nm-input nm-input--sm"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="4" readOnly /></div></div><div className="nm-input nm-input--sm"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} placeholder="-" readOnly /></div></div><div className="nm-input nm-input--sm"><div className="nm-input__container" data-state="focus"><input className="nm-input__field" maxLength={1} placeholder="-" readOnly /></div></div><div className="nm-input nm-input--sm"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} placeholder="-" readOnly /></div></div></div></div><div className="desc">Don't move focus unpredictably or trap keyboard users — Tab and Shift+Tab must still work.</div></div>
</div>

<div className="dodont">
  <div className="card do"><div className="bar"><span className="pill">✓</span>Do</div><div className="frame"><div className="code-field"><div className="nm-input nm-input--sm"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="4" readOnly /></div></div><div className="nm-input nm-input--sm"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="2" readOnly /></div></div><div className="nm-input nm-input--sm"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="9" readOnly /></div></div><div className="nm-input nm-input--sm"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="1" readOnly /></div></div></div></div><div className="desc">Show errors only once the code is complete, or on submit.</div></div>
  <div className="card dont"><div className="bar"><span className="pill">✕</span>Don't</div><div className="frame"><div className="code-field"><div className="nm-input nm-input--sm nm-input--error"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="4" readOnly /></div></div><div className="nm-input nm-input--sm nm-input--error"><div className="nm-input__container" data-state="focus"><input className="nm-input__field" maxLength={1} placeholder="-" readOnly /></div></div><div className="nm-input nm-input--sm nm-input--error"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} placeholder="-" readOnly /></div></div><div className="nm-input nm-input--sm nm-input--error"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} placeholder="-" readOnly /></div></div></div></div><div className="desc">Don't validate while the user is still typing partial input.</div></div>
</div>

<div className="dodont">
  <div className="card do"><div className="bar"><span className="pill">✓</span>Do</div><div className="frame"><div style={{display:'flex',flexDirection:'column',alignItems:'flex-start',gap:'8px'}}><div className="code-field"><div className="nm-input nm-input--sm nm-input--error"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="4" readOnly /></div></div><div className="nm-input nm-input--sm nm-input--error"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="2" readOnly /></div></div><div className="nm-input nm-input--sm nm-input--error"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="9" readOnly /></div></div><div className="nm-input nm-input--sm nm-input--error"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="1" readOnly /></div></div></div><div className="nm-input__help">Code expires in <b>4:55</b></div><div className="nm-input__help nm-input--error" style={{color:'var(--nm-input-help-fg)', display:'flex', alignItems:'center', gap:'4px'}}><Ic n="alert-circle" /> Incorrect code.</div></div></div><div className="desc">Keep help text — like a resend countdown — visible, and show the error beneath it.</div></div>
  <div className="card dont"><div className="bar"><span className="pill">✕</span>Don't</div><div className="frame"><div style={{display:'flex',flexDirection:'column',alignItems:'flex-start',gap:'8px'}}><div className="code-field"><div className="nm-input nm-input--sm nm-input--error"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="4" readOnly /></div></div><div className="nm-input nm-input--sm nm-input--error"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="2" readOnly /></div></div><div className="nm-input nm-input--sm nm-input--error"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="9" readOnly /></div></div><div className="nm-input nm-input--sm nm-input--error"><div className="nm-input__container"><input className="nm-input__field" maxLength={1} defaultValue="1" readOnly /></div></div></div><div className="nm-input__help nm-input--error" style={{color:'var(--nm-input-help-fg)', display:'flex', alignItems:'center', gap:'4px'}}><Ic n="alert-circle" /> Incorrect code.</div></div></div><div className="desc">Don't replace helpful information — like the countdown — with the error message.</div></div>
</div>

</section>

<section className="block" id="a11y">

## Accessibility

#### Semantics

- Use `<input inputmode="numeric" pattern="[0-9]*" maxlength="1">` per cell so mobile keyboards default to a numeric pad.
- Wrap the row in a `<fieldset>` with a `<legend>` describing what the code is for ("Verification code sent to your phone").

#### Keyboard

- Auto-advance and auto-retreat (described in [Behavior](#behavior)) must not trap focus — Tab and Shift+Tab should still move a keyboard user through cells normally if they choose not to type sequentially.
- Paste-splitting must not break the browser's native paste handling for users who paste one cell at a time.

#### Labeling

- Announce validation results in an `aria-live="polite"` region near the field — don't rely on color or shake animation alone.
- Give the fieldset an `aria-describedby` pointing at any help or countdown text ("Code expires in 4:59").

</section>

<section className="block" id="sandbox">

## Playground

<CodeSandbox />

</section>

<section className="block" id="code-usage">

## Implementation

```html
<link rel="stylesheet" href="namirial-theme.css">

<fieldset>
  <legend>Verification code</legend>
  <div class="code-field">
    <div class="nm-input nm-input--lg">
      <div class="nm-input__container">
        <input class="nm-input__field" inputmode="numeric" pattern="[0-9]*" maxlength="1" placeholder="-">
      </div>
    </div>
    <!-- repeat per digit -->
  </div>
</fieldset>
```

#### CSS classes

| Class | Values | Default |
|-------|--------|---------|
| `nm-input--{size}` | sm · lg per cell — the only two sizes code supports | `lg` |
| `nm-input--{intent}` | error · success, applied to every cell (or the last, for success) | — |
| `code-field` | Flex row with an 8px gap and fixed cell width | — |

</section>
