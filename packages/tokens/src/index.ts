/**
 * @namirial/design-system — Design tokens (W3C + CSS vars + TS).
 * All tokens are derived from Figma; no manual authoring.
 */
export type { DesignTokens, TokenGroup } from "./types.js";
export { tokens } from "./tokens.js";
