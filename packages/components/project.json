{
  "name": "components",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/components/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec stencil build",
        "cwd": "packages/components"
      },
      "configurations": { "stable": {}, "experimental": {} }
    }
  }
}
