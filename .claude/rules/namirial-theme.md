# namirial-theme.css — Protected Reference File

`namirial-theme.css` is a protected reference file and must be treated as read-only.

## Mandatory rules

- NEVER modify `namirial-theme.css`.
- NEVER delete, rename, move, overwrite, reformat, regenerate, or replace `namirial-theme.css`.
- NEVER apply automated formatting, lint fixes, migrations, refactors, or cleanup operations to `namirial-theme.css`.
- NEVER include `namirial-theme.css` among files changed as part of a task.
- You MAY read and inspect `namirial-theme.css`.

## Reference behavior

`namirial-theme.css` is the canonical reference for CSS generated in this project.

When creating or modifying other CSS files:

- Always inspect `namirial-theme.css` before generating new CSS when its conventions are relevant.
- Follow its conventions, structure, organization, naming patterns, formatting, selector style, property ordering, and general implementation approach.
- Prefer consistency with `namirial-theme.css` over introducing alternative stylistic patterns.
- Do not copy code mechanically when it is not appropriate; use it as the authoritative implementation and style guideline.

## Conflict resolution

If a task appears to require modifying `namirial-theme.css`, DO NOT modify it.

Instead:
1. Leave `namirial-theme.css` unchanged.
2. Implement the required change elsewhere when possible.
3. Inform the user if the requested task cannot be completed without changing `namirial-theme.css`.

Protection of `namirial-theme.css` takes precedence over normal refactoring, formatting, cleanup, migration, or implementation instructions.